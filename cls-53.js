//access object 
// two method are used access object method
//first
var point={
    x:10,
    y:20,
    z:30
}

// method one
console.log(point.x)
console.log(point.y)
console.log(point.z)
// method two
console.log(point["x"])
console.log(point["y"])
console.log(point["z"])

